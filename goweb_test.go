package goweb

import (
	"testing"
)

// TestSomething doesn't actually test anything (weird eh?) but it causes our
// test packages to actually build (and therefore validate) this package.
func TestSomething(t *testing.T) {}
